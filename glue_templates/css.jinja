/* glue: {{ version }} hash: {{ hash }} */
{% for image in images %}.{{ image.label }}{{ image.pseudo }}{%- if not image.last %}, {%- endif %}{%- endfor %}{
  background-image: url('{{ sprite_path }}');
  background-repeat: no-repeat;
  display: inline-block;
}
{% for image in images %}
.{{ image.label }}{{ image.pseudo }}{
  background-position:{{ image.x ~ ('px' if image.x) }} {{ image.y ~ ('px' if image.y) }};
  width: {{ image.width }}px;
  height: {{ image.height }}px;
}{% endfor %}{% if ratios|length > 1 %}{% for ratio in ratios %}
{% if not ratios[ratio].ratio == 1.0 %}@media screen and (-webkit-min-device-pixel-ratio: {{ ratios[ratio].ratio }}), screen and (min--moz-device-pixel-ratio: {{ ratios[ratio].ratio }}),screen and (-o-min-device-piratio: {{ ratios[ratio].ratio }})),screen and (min-device-pixel-ratio: {{ ratios[ratio].ratio }})
    {% for image in images %}.{{ image.label }}{{ image.pseudo }}{% if not image.last %}
    {% endif %}{% endfor %}
        background-image: url({{ ratios[ratio].sprite_path }});
        background-size: {{ width }}px {{ height }}px;
{% endif %}
{% endfor %}
{% endif %}